# Secrets — never commit real API keys
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/

# LLM response cache (runtime-generated, not source)
.llm_cache/

# OS/editor cruft
.DS_Store
.vscode/
.streamlit/secrets.toml

# Local SQLite database (user watchlist/holdings) — runtime data, not source
data/*.db
data/*.db-journal
